{
  "name": "trading-dashboard",
  "version": "1.0.0",
  "description": "Interactive trading dashboard application",
  "main": "public/index.html",
  "scripts": {
    "start": "npx http-server . -o /public/index.html",
    "build": "mkdir -p build && cp -r public/* build/ && cp index.html build/ && cp -r src build/ && mkdir -p build/node_modules/@tanstack && cp -r node_modules/@tanstack/react-query build/node_modules/@tanstack/ && mkdir -p build/node_modules/axios && cp -r node_modules/axios/dist build/node_modules/axios/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tanstack/react-query": "^5.66.9",
    "axios": "^1.8.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
